# cold-run
A URL-based tool tests a prototype or feature using an LLM browser agent with personas. It explores the product as a cold user, records interactions and screenshots, and returns UX and product-quality feedback from the perspective of a user in the target demographic.
